# API reference compatibility page

The API reference moved to component pages generated from every top-level
`@PublicAPI` annotation. Continue with the [API reference](reference/index.md).

This compatibility page preserves the published `/api/` URL.
