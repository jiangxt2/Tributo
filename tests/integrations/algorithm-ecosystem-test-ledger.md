# Algorithm Ecosystem Test Ledger

This ledger tracks non-duplicated evidence for the algorithm decomposition and
official Wheel delivery change. A passed long-running suite is not repeated
unless relevant production code, test code, infrastructure, configuration, or
the execution environment changes.

| Suite | Scope | Code state | Reason | Result |
| --- | --- | --- | --- | --- |
| Core decomposition contracts | Strategy serialization, Builder, Planner, Receipt, plugin ownership | Current worktree | Fast contract baseline | Passed: 58 tests plus support-evidence and conformance contract coverage |
| Core legacy compatibility | Existing Torch recipe, collective, plugin, input lifecycle | Current worktree | Protect existing public paths | Passed: 40 recipe/plugin tests and 29 input tests |
| Decomposition local Ray | Joblib estimator tasks, ensemble units, iterative rounds/checkpoint, resume, corruption rejection | Current worktree | Development diagnosis before cluster IT | Passed: 5 tests with local Ray; resume and fail-closed corruption paths included |
| Official Wheel unit/static/build | All 14 packages, 27 official Entry Points, public SPI, Descriptor-only discovery, mypy, Ruff, independent Wheel build | Current Core plus official Monorepo | Prove package boundaries before cluster installation | Passed: 68 tests including the X-Learner ONNX exporter contract; 27-entry-point Conformance; Ruff and mypy(packages); all Wheels built |
| Official Wheel local end-to-end | Ray Data, decomposition runtimes, RecipeV2 DDP, Graph/XGBoost Framework Native, causal MapReduce, finite staged Framework Native, checkpoint, ONNX/Safetensors/report Bundle | Current Core plus installed fixed Wheels | Diagnose runtime and exporter boundaries | Passed prior category paths plus out-of-tree XGBoost, five-stage X-Learner with typed safe batch Flavor, and three-stage AIPW/DR with distributed nuisance models, finite ATE, stage evidence, report and ONNX Bundle |
| Distributed algorithm Docker IT | All official categories, RF Joblib/Native, LR iterative, cross-node Bundle/inference, Tune, recovery, failure injection | Final candidate before Gate | Required cross-node execution proof | Blocked after final attempts: official Ray Job exceeded pytest 1200s while serially running the 27-record gate; every run cleaned its Compose project. Earlier targets and individual local proofs passed. |
| Priority six-algorithm Docker IT | XGBoost, RF Joblib, LR iterative, Linear DML, DR/AIPW, X-Learner; cross-node Bundle/ONNX, baseline, checkpoint recovery, and batch inference | Current priority Gate change | Requested focused evidence after the 27-record timeout | Passed: 1 test in 431.19s on a 3-node Docker Ray cluster; all six records reported distributed/cluster_distributed/cross-node execution and ONNX artifacts; RF/LR baseline, checkpoint recovery/corruption rejection, and 16-row two-node batch inference passed. The Gate uses two folds for DR/X-Learner to bound nested Trainer pressure; implementations retain five-fold defaults. |
| Final static and unit suite | Cumulative Ruff, mypy, Core regression, official Wheel tests, Conformance | Final implementation candidate | Final merge-readiness evidence | Core affected tests `17 passed`; official boosting/DR/X-Learner tests `10 passed`; Ruff and mypy affected modules passed; priority Docker Gate passed. The 27-record full Gate remains a separate scope that previously timed out. |
| Current 37-algorithm distributed training and inference Docker IT | Six non-overlapping category Ray Jobs covering 15 Wheels and 37 Entry Points; two-node training receipts, Bundle-backed inference, real Parquet ResultSink and two-node Ray actors | Core Gate `e471773f0007cf5243e25421dd579cb2a9378733` (including scalar contract parent `c9640cf8e57bab97833834ae7bc7b11bcf20220c`) with algorithms `b5c1405b43813b82d1e0096d5c32694451f7b5de` | Replace the stale 27-record completion criterion with one fixed-revision certification Gate | Passed: `1 passed in 1337.48s` on project `tributo-distributed-algorithm-it-20260831141344-50825`. All 37 unique Entry Points succeeded with category counts 13/3/6/5/8/2; every receipt recorded two training workers on two nodes, `distributed=true`, `cluster_distributed=true`, `cross_node=true`, and zero driver-materialized training rows. Every formal Bundle inference wrote and read back 16 Parquet rows with two actors on two nodes and valid 64-character manifest/result digests. All 15 Wheels were installed from the fixed revisions. Runtime image ID `sha256:81a47690470db518810d0c485fc37f67ca31601ec3c1f0b43f9dc7bb564731f9` was verified, no dangling image was added, and all project-owned containers, network and volumes were removed. Logs: `/tmp/tributo-distributed-algorithm-it-20260831141344-50825/`. A prior approved invocation stopped before Docker setup because the Gate worktree environment lacked `packaging`; after `uv sync --locked --group dev`, the single full Docker execution above produced the certification result. |
| Post-review certification hardening | Resolver rank/layout preflight; Graph topology/feature/logits provenance and corrupt-checkpoint rejection; source-free scalar Core check; immutable Entry Point/distribution/algorithm/implementation identity; pre-Docker and final provenance; clean-source enforcement; 16-Wheel SHA-256 and runtime provenance | Core base `6ef79841261d88c98bab420193afbe58ca6baa28`, Gate `e40949bfac87de129989e38bbe63b036fe3c47af`, and algorithms `57f268423e9fbd51a65e361699cdd7cbd6361000`; versions remain Core `1.0.0` and algorithms `0.1.0` under the approved first-release baseline | Close all Medium/Low review findings without weakening the prior runtime contracts | Fast validation passed: Core affected inference `82 passed`, resolver mypy/Ruff and full pre-check passed with only the pre-existing destructor warning; algorithms post-remediation full suite `152 passed`, mypy on 103 source files, full Ruff/format, Graph corruption boundaries, all 15 earlier Wheel/sdist builds and isolated Core/Classical source-free install with 13 Entry Points plus scalar contract passed; Gate provenance/runner/full-identity contracts `10 passed`, affected provenance/matrix/Ray Job/contract mypy and Ruff plus Bash syntax passed. The first full algorithms run loaded the old Core and failed only the scalar contract; the second completed all 152 tests but sandbox-denied pytest cache teardown; the recorded passing run used the scalar Core source and disabled pytest cache. The post-remediation Docker certification is recorded in the following row; the preceding 37/37 result remains historical evidence only for its recorded fixed revisions. |
| Post-review 37-algorithm Docker run before output-shape hardening | Six non-overlapping category Ray Jobs covering all 15 official Wheels and 37 immutable Entry Point/distribution/algorithm/implementation identities; cross-node training, formal Bundle inference, Parquet sink, preflight/final provenance, and scoped cleanup | Clean Core Gate `e40949bfac87de129989e38bbe63b036fe3c47af` on base `6ef79841261d88c98bab420193afbe58ca6baa28`, with clean algorithms `57f268423e9fbd51a65e361699cdd7cbd6361000` | Single authorized run after the preceding review remediation | Runtime evidence passed: `1 passed in 1350.24s (0:22:30)` on project `tributo-distributed-algorithm-it-20260901103757-93038`. All 37 unique records succeeded with exact category counts 13/3/6/5/8/2; all six inference summaries reported `all_distributed=true`. Training receipts proved two workers on two nodes with no driver materialization; each formal Bundle inference wrote and read back 16 Parquet rows using two actors on two nodes. Core plus 15 algorithm Wheel SHA-256 values were recorded. Runtime image ID `sha256:81a47690470db518810d0c485fc37f67ca31601ec3c1f0b43f9dc7bb564731f9`, Python `3.12.9`, and Ray `2.55.1` were verified. Preflight configuration SHA-256 is `a7849ee5e23387a796823b5a5158aebfd889049e9d7f2c00c8c4245f63320cd3`; final configuration SHA-256 is `048e00e71b2b88c0673dba2374808b56692a5f2859dce422df01d8a5d3492bc4`, bound to preflight document SHA-256 `9a9dbe6f9dfc443e191b5e859e7f339425859a5458c31890ed8c220a86365b4f`. No dangling or `<none>` image was added; all project-owned containers, network, and volumes were removed. Subsequent static review found that the Gate did not assert manifest output trailing shape, so this result remains credible runtime evidence but does not certify the complete dtype/shape acceptance criterion. Logs and provenance: `/tmp/tributo-distributed-algorithm-it-20260901103757-93038/`. |
| Output-contract and receipt assertion hardening | Preserve each manifest output dtype and trailing shape through Parquet materialization; validate row rank, fixed dimensions, dtype and finite values; assert the serialized `cross_node` receipt field | Candidate derived from Core Gate `e40949bfac87de129989e38bbe63b036fe3c47af`, with algorithms fixed at `57f268423e9fbd51a65e361699cdd7cbd6361000`; the commit containing this row fixes the remediation revision | Close the final Medium/Low review findings without shrinking the documented Gate contract | Fast validation passed: output-contract and Gate static suite `17 passed`; Ruff and format passed on all four affected files; mypy passed on the three directly affected output-contract/Gate modules. Full-file mypy on `test_dnn_pu_training.py` still reports its two pre-existing `AlgorithmArtifact.mode` typing findings at lines 573 and 623; the only change in that file is the typed-dictionary-style `cross_node` assertion. One separately approved final Docker certification remains required after this candidate is committed and clean. |
