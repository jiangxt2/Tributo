"""Collection-only MLflow stub for the host-side Model Export preflight.

The real Docker suite never adds this directory to ``PYTHONPATH`` and imports
the locked MLflow distribution from the integration-test runtime image.
"""
